# Rules

- Do NOT run `git push` automatically or proactively without the user's explicit request or permission.
- Unless explicitly permitted by the user, all UI/UX changes or feature modifications should apply ONLY to the PWA (the installed program) and should NOT affect the regular website experience.
