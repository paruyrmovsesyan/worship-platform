/* ══════════════════════════════════════════════
   SONGS PAGE  –  div + CSS Grid (no <table>)
   ══════════════════════════════════════════════ */

.songs-page {
  padding: 80px 40px 60px;
  max-width: 1200px;
  margin: 0 auto;
  min-height: 100vh;
}

/* ── Header ── */
.songs-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  flex-wrap: wrap;
  gap: 14px;
}

.songs-title {
  font-size: 1.8rem;
  font-weight: 800;
  letter-spacing: -0.5px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.count-badge {
  font-size: 0.82rem;
  font-weight: 500;
  color: #6E6E80;
  background: rgba(255,255,255,0.05);
  padding: 3px 10px;
  border-radius: 99px;
}

.songs-controls {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
}

/* ── Search ── */
.search-box {
  position: relative;
  display: flex;
  align-items: center;
}
.search-box svg {
  position: absolute;
  left: 11px;
  color: #6E6E80;
  pointer-events: none;
}
.search-box input {
  width: 240px;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.09);
  padding: 8px 30px 8px 32px;
  border-radius: 10px;
  color: #fff;
  font-size: 0.84rem;
  font-family: inherit;
  outline: none;
  transition: border-color .2s;
}
.search-box input:focus { border-color: rgba(0,240,255,.4); }
.search-box input::placeholder { color: #44445A; }
.search-x {
  position: absolute;
  right: 8px;
  background: none;
  border: none;
  color: #555570;
  cursor: pointer;
  font-size: .72rem;
  padding: 2px;
  line-height: 1;
  transition: color .2s;
}
.search-x:hover { color: #fff; }

/* ── Sort ── */
.sort-sel {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.09);
  color: #A0A0B0;
  border-radius: 10px;
  padding: 8px 12px;
  font-size: 0.82rem;
  font-family: inherit;
  cursor: pointer;
  outline: none;
}

/* ── Key pills ── */
.key-pills {
  display: flex;
  gap: 5px;
  flex-wrap: wrap;
  margin-bottom: 18px;
}
.kp {
  padding: 4px 12px;
  border-radius: 999px;
  font-size: 0.73rem;
  font-weight: 600;
  font-family: inherit;
  border: 1px solid rgba(255,255,255,0.07);
  background: transparent;
  color: #6E6E80;
  cursor: pointer;
  transition: all .15s;
  white-space: nowrap;
}
.kp:hover { border-color: rgba(255,255,255,.2); color: #ccc; }
.kp.active {
  border-color: rgba(0,240,255,.4);
  background: rgba(0,240,255,.08);
  color: #00F0FF;
}

/* ══════════════════════════════════════════════
   SONG LIST  —  CSS Grid rows
   ══════════════════════════════════════════════ */
.song-list-wrap {
  background: #111119;
  border: 1px solid rgba(255,255,255,0.07);
  border-radius: 16px;
  overflow: hidden;
}

/*
  Grid columns (desktop):
  [avatar+name]  [key]  [bpm]  [artist]  [date]  [action]
     1fr         68px   60px    160px    120px    84px
*/
.song-row {
  display: grid;
  grid-template-columns: 1fr 68px 60px 160px 120px 84px;
  align-items: center;
}

/* Header row */
.header-row {
  padding: 0;
  border-bottom: 1px solid rgba(255,255,255,0.07);
}
.header-row > div {
  padding: 12px 16px;
  font-size: 0.7rem;
  font-weight: 700;
  color: #44445A;
  letter-spacing: .07em;
  text-transform: uppercase;
  white-space: nowrap;
}

/* Data rows */
.data-row {
  border-bottom: 1px solid rgba(255,255,255,0.04);
  cursor: pointer;
  transition: background .15s;
}
.data-row:last-child { border-bottom: none; }
.data-row:hover { background: rgba(255,255,255,0.025); }
.data-row > div { padding: 10px 16px; }

/* ── Columns ── */
.col-name {
  display: flex;
  align-items: center;
  gap: 12px;
  min-width: 0;
  overflow: hidden;
}

.s-avatar {
  width: 36px;
  height: 36px;
  border-radius: 8px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: .95rem;
  color: rgba(255,255,255,.9);
}

.s-name-block {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.s-title {
  font-size: .88rem;
  font-weight: 600;
  color: #fff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.s-artist-sm {
  font-size: .72rem;
  color: #44445A;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  margin-top: 2px;
}

.col-key {
  white-space: nowrap;
  overflow: hidden;
}

.key-badge {
  display: inline-block;
  background: rgba(0,240,255,.08);
  color: #00D4FF;
  border: 1px solid rgba(0,240,255,.22);
  padding: 2px 8px;
  border-radius: 6px;
  font-size: .7rem;
  font-weight: 700;
  letter-spacing: .03em;
}

.col-bpm, .col-artist, .col-date {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.dim {
  color: #55556E;
  font-size: .82rem;
}

.col-act {
  text-align: right;
}

.open-btn {
  background: transparent;
  border: 1px solid rgba(255,255,255,.08);
  color: #6060A0;
  padding: 5px 12px;
  border-radius: 99px;
  font-size: .76rem;
  font-weight: 500;
  font-family: inherit;
  cursor: pointer;
  opacity: 0;
  transition: all .18s;
  white-space: nowrap;
}
.open-btn { transition: background .2s, border-color .2s; }
.open-btn:hover { background: rgba(0,240,255,.1); border-color: rgba(0,240,255,.3); }

/* Favorite button styling and animation */
.fav-btn {
  transition: transform 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}
.fav-btn:hover {
  transform: scale(1.15);
}
.fav-btn:active {
  transform: scale(0.9);
}
.fav-btn.is-fav svg {
  animation: heartBeat 0.35s ease-out;
}

@keyframes heartBeat {
  0% { transform: scale(1); }
  50% { transform: scale(1.3); }
  100% { transform: scale(1); }
}

.data-row:hover .open-btn {
  opacity: 1;
  background: rgba(0,240,255,.07);
  border-color: rgba(0,240,255,.3);
  color: #00F0FF;
}

.list-placeholder {
  padding: 48px;
  text-align: center;
  color: #44445A;
  font-size: .9rem;
}

/* ── Gradient avatars ── */
.bg-purple { background: linear-gradient(135deg,#4A00E0,#8E2DE2); }
.bg-blue   { background: linear-gradient(135deg,#0052D4,#4364F7); }
.bg-cyan   { background: linear-gradient(135deg,#00B4DB,#0083B0); }
.bg-gold   { background: linear-gradient(135deg,#F37335,#FDC830); }

/* ══════════════════════════════════════════════
   RESPONSIVE  –  hide 3 right cols on small screens
   ══════════════════════════════════════════════ */
@media (max-width: 860px) {
  .song-row {
    grid-template-columns: 1fr 68px 84px;
  }
  .desk-only { display: none !important; }
  .open-btn  { opacity: 1; }
}

@media (max-width: 600px) {
  .songs-page { padding: 76px 16px 100px; }
  .songs-header { flex-direction: column; align-items: flex-start; }
  .search-box input { width: 200px; }
  .song-row { grid-template-columns: 1fr 60px 72px; }
}

.desk-hide { display: none; }
@media (max-width: 860px) { .desk-hide { display: block; } }
